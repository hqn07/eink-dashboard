// secrets.h.example — copy to secrets.h (gitignored) and fill in.
// Never commit secrets.h.
#pragma once

const char* ssid        = "YOUR_WIFI_SSID";
const char* password    = "YOUR_WIFI_PASSWORD";

// Your dashboard server. Change to your Railway URL once deployed.
//   "http://192.168.1.42:3000"            <- local Node dev server
//   "https://your-app.up.railway.app"     <- deployed
const char* serverBase  = "https://your-app.up.railway.app";

// Optional shared secret. Must match DEVICE_TOKEN env var on the server.
const char* deviceToken = "";
